# Template for Streamlit Secrets (Paste into Streamlit Cloud -> Settings -> Secrets)

# 1. Google Service Account Credentials (REQUIRED)
# Paste the entire JSON content from Data/credentials.json inside single quotes:
# GOOGLE_CREDENTIALS = '{"type": "service_account", "project_id": "...", ...}'

# 2. User Authentication (For Cloud Login)
# Option A: Multiple users
# [users]
# admin = "MatKhauBaoMat2025"
# advisor1 = "DtkAdvisor@123"

# Option B: Single admin user
# APP_USERNAME = "admin"
# APP_PASSWORD = "MatKhauBaoMat2025"
